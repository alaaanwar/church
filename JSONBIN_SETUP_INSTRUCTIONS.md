# تعليمات إعداد JSONBin.io للتطبيق

## الخطوة 1: إنشاء حساب JSONBin.io

1. افتح المتصفح واذهب إلى: https://jsonbin.io
2. اضغط على "Sign Up" أو "Get Started Free"
3. سجل حساب جديد (مجاني)
4. سجل دخول

## الخطوة 2: الحصول على API Key

1. بعد تسجيل الدخول، اذهب إلى: https://jsonbin.io/api-keys
2. اضغط على "Create Access Key"
3. اكتب اسم للـ Key (مثل: "Church App Key")
4. انسخ الـ API Key (يبدأ بـ $2a$10$...)
5. **مهم جداً:** احتفظ بهذا الـ Key في مكان آمن

## الخطوة 3: إنشاء Bin للأسئلة

1. اذهب إلى: https://jsonbin.io/app/bins
2. اضغط على "Create New Bin"
3. في الـ JSON Editor، احذف المحتوى واكتب:
```json
[]
```
4. اضغط "Create"
5. **انسخ الـ Bin ID** من الرابط في المتصفح (مثلاً: `67890abcdef12345`)

## الخطوة 4: إنشاء Bin للمستخدمين

1. كرر نفس العملية (Create New Bin)
2. اكتب في الـ JSON Editor:
```json
[]
```
3. اضغط "Create"
4. **انسخ الـ Bin ID الثاني**

## الخطوة 5: تحديث ملف api_config.dart

1. افتح الملف: `lib/config/api_config.dart`
2. استبدل القيم التالية بالقيم الخاصة بك:

```dart
class ApiConfig {
  // ضع الـ API Key هنا (من الخطوة 2)
  static const String jsonBinApiKey = '\$2a\$10\$YOUR_ACTUAL_API_KEY_HERE';
  
  // ضع Bin ID الخاص بالأسئلة (من الخطوة 3)
  static const String questionsBinId = 'YOUR_QUESTIONS_BIN_ID_HERE';
  
  // ضع Bin ID الخاص بالمستخدمين (من الخطوة 4)
  static const String usersBinId = 'YOUR_USERS_BIN_ID_HERE';
  
  static const String baseUrl = 'https://api.jsonbin.io/v3';
}
```

## الخطوة 6: تشغيل التطبيق

1. احفظ الملف
2. في Terminal اكتب:
```bash
flutter run -d windows
```

## ✅ الآن كل شيء أونلاين!

- ✅ الأسئلة تُضاف/تُعدّل/تُحذف أونلاين
- ✅ المستخدمين يُحفظون أونلاين
- ✅ جميع الأدمنز يشوفون نفس البيانات
- ✅ لا حاجة لإرسال التطبيق من جديد للمستخدمين

## ⚠️ ملاحظة أمنية

- لا تشارك الـ API Key مع أحد
- JSONBin المجاني يدعم حتى 100,000 request شهرياً (كافي جداً)
- إذا احتجت capacity أكبر، اشترك في الباقة المدفوعة

## مشاكل محتملة وحلولها

### خطأ: "Failed to save questions/users"
- تأكد من صحة الـ API Key
- تأكد من صحة الـ Bin IDs
- تأكد من اتصال الإنترنت

### البيانات لا تظهر
- تأكد من أن الـ Bins تم إنشاؤها بصيغة JSON صحيحة (قائمة فارغة [])
- افحص الـ bins على الموقع للتأكد من وجود البيانات

---

**بالتوفيق! 🎉**
